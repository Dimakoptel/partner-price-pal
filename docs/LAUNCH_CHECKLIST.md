# ЧЕК-ЛИСТ ЗАПУСКА: Модуль Продажи — MES COZY ART

## ✅ ПОДГОТОВКА

- [ ] Миграции применены (все таблицы существуют в базе)
- [ ] Справочники заполнены (`dictionary_types` + `dictionary_items`: статусы заказов, типы, способы доставки, этапы производства)
- [ ] `system_settings` загружены: `warranty_default_months`, `discount_approval_threshold`, `reserve_hours_unpaid`, `reserve_hours_paid`
- [ ] Edge Functions развёрнуты: `webhook-wordpress-lead`, `generate-document`, `seed-sales-demo`
- [ ] Секрет `WORDPRESS_WEBHOOK_KEY` установлен (если WordPress интеграция нужна)
- [ ] RLS-политики проверены (менеджер видит только свои заказы, админ — все)
- [ ] Создан минимум один пользователь с ролью `admin`

## ✅ ТЕСТИРОВАНИЕ

### Клиенты
- [ ] Создан тестовый клиент типа B2C
- [ ] Создан тестовый клиент типа «Агент» с `commission_rate = 10`

### Лиды → Заказы
- [ ] Создан лид вручную (статус `new`)
- [ ] Лид конвертирован в клиента (`client_id` привязан)
- [ ] Лид конвертирован в заказ (статус лида → `won`, создан `orders` + привязка `lead_id`)

### Заказы
- [ ] В заказ добавлены 2–3 позиции через `OrderItemsEditor`
- [ ] Сумма заказа пересчитывается автоматически (`recalculate_order_totals`)
- [ ] Скидка >15% переводит заказ в статус `pending_approval`
- [ ] Номер заказа генерируется автоматически (`ORD-YYYYMMDD-NNN`)

### Документы
- [ ] Сгенерирован счёт на оплату (HTML)
- [ ] Сгенерирован гарантийный талон (срок гарантии корректный)

### Комиссии
- [ ] При заказе через агента создаётся запись в `agent_commissions` со статусом `reserved`

### Резервирование
- [ ] Проверка доступности отображается при добавлении позиции
- [ ] `reserve_order_stock` создаёт записи в `reservations`

## ✅ ИНТЕГРАЦИИ

- [ ] **WordPress webhook:** тестовый POST создаёт лид и клиента
- [ ] **Очередь 1С:** `integration_queue` — записи видны на `/admin/integrations`
- [ ] **Retry:** кнопка «Повторить» для записей со статусом `error` работает

## ✅ ПРОИЗВОДСТВО

- [ ] `create_production_order_from_sales` создаёт производственный заказ с этапами
- [ ] Завершение всех этапов переводит заказ в статус `ready`

## ✅ ДОКУМЕНТАЦИЯ

- [ ] `KNOWLEDGE.md` — актуален
- [ ] `docs/SALES_MODULE.md` — описание модуля
- [ ] `docs/ACCEPTANCE_SALES.md` — приёмочные критерии
- [ ] `DEPLOY.md` — инструкция развертывания

## ✅ ГОТОВНОСТЬ КОМАНДЫ

- [ ] Менеджеры обучены работе с модулем
- [ ] Назначены ответственные за каждый раздел
- [ ] Создан Telegram-чат для поддержки запуска

## 📊 МЕТРИКИ КАЧЕСТВА

| Метрика | Целевое значение |
|---------|-----------------|
| Загрузка списка заказов | < 2 сек |
| Пересчёт суммы заказа | < 100 мс |
| Генерация документа | < 3 сек |
| Ошибки в консоли | 0 |

---

**Дата запуска:** _______________

**Ответственный:** _______________

**Статус:** [ ] Готов к запуску · [ ] Требует доработки · [ ] Отложен
